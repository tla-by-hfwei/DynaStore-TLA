\* TLC Model Configuration for WeakSnapshot
\* Small model for quick verification

\* Model values
CONSTANT 
    P = {p1, p2, p3}
    Values = {v1, v2}

\* Symmetry sets for optimization
SYMMETRY
    P
    Values

\* Check properties
INVARIANTS
    TypeOK
    Integrity

\* State constraint to limit state space
CONSTRAINT
    Len(ops) <= 6

\* Specification to check
SPECIFICATION Spec

\* Properties
PROPERTIES
    \* PR3: Monotonicity of scans
    \* PR4: Non-empty intersection
